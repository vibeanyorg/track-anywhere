# Use only for the one-shot migration container. Never add this DSN to the
# long-running Dokploy Application environment.
TRACK_ANYWHERE_DATABASE_URL=postgresql+psycopg://track_anywhere_migrator:MIGRATOR_PASSWORD@POSTGRES_HOST:5432/track_anywhere
TRACK_ANYWHERE_DB_RUNTIME_ROLE=track_anywhere_runtime
